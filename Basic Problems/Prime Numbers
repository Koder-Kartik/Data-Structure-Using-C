#include<stdio.h>
int main()
{
    int n, i=2;
    printf("enter the number");
    scanf("%d",&n);
    while(i<n)
    {
        if(n%i==0)
        {
            printf("not prime");
            break;
        }
        i++;

    }
    if(i==n)
    printf("prime no");
    return 0;
}
